---
// Wrapper de compatibilidad para páginas auxiliares históricas de Unidad 1.
import AcademicUnitNav from "./AcademicUnitNav.astro";
import { getLocalizedAcademicUnit } from "../../data/physics/index.js";

const { activeSlug = null, locale = "es" } = Astro.props;
const unit = getLocalizedAcademicUnit(1, locale);
---

<AcademicUnitNav unit={unit} activeSlug={activeSlug} locale={locale} />
