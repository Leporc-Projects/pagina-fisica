import { assertSupportedLocale } from "../../../i18n/config.js";
import { t } from "../../../i18n/index.js";
import { generateFamilyInstance } from "../../../utils/exercise-families.js";
import { UNIT_3_EXERCISE_FAMILIES } from "./families.js";
import { FAMILY_OBJECTIVES_EN, FAMILY_PRESENTERS_EN } from "./i18n/families.en.js";
const required=(value,context)=>{if(value==null||value==="")throw new RangeError(`Missing English Unit 3 family translation: ${context}`);return value;};
const parallel=(source,translated,context)=>{if(!Array.isArray(translated)||source.length!==translated.length)throw new RangeError(`English Unit 3 family translation changed structure: ${context}`);return translated;};
export const localizeUnit3ExerciseFamily=(family,locale)=>{assertSupportedLocale(locale);if(!family||locale==="es")return family;return{...family,objectives:parallel(family.objectives,required(FAMILY_OBJECTIVES_EN[family.id],`${family.id}.objectives`),`${family.id}.objectives`),feedback:{...family.feedback,correct:t(locale,"exercise.familyFeedback.correct"),incorrect:t(locale,"exercise.familyFeedback.incorrect")}};};
export const getLocalizedUnit3ExerciseFamilies=(locale)=>UNIT_3_EXERCISE_FAMILIES.map((family)=>localizeUnit3ExerciseFamily(family,locale));
const localizeInstance=(instance,locale)=>{if(locale==="es")return instance;const translated=required(FAMILY_PRESENTERS_EN[instance.familyId],`${instance.familyId}.presenter`)(instance);const family=localizeUnit3ExerciseFamily(UNIT_3_EXERCISE_FAMILIES.find((candidate)=>candidate.id===instance.familyId),locale);const solution=parallel(instance.solution,translated.solution,`${instance.familyId}.solution`);let interaction;if(instance.interaction.kind==="singleChoice"){const options=parallel(instance.interaction.options,translated.options,`${instance.familyId}.options`);interaction={...instance.interaction,options:instance.interaction.options.map((option,index)=>({...option,content:options[index]}))};}else{const fields=instance.interaction.kind==="number"?[instance.interaction.field]:instance.interaction.fields;const labels=parallel(fields,translated.fields,`${instance.familyId}.fields`);const localized=fields.map((field,index)=>({...field,label:labels[index]}));interaction=instance.interaction.kind==="number"?{...instance.interaction,field:localized[0]}:{...instance.interaction,fields:localized};}return{...instance,objectives:family.objectives,title:translated.title,prompt:translated.prompt,hints:parallel(instance.hints,translated.hints,`${instance.familyId}.hints`),solution:instance.solution.map((step,index)=>({...step,title:["Model","Representation","Calculation","Interpretation"][index]??`Step ${index+1}`,text:solution[index]})),answer:{...instance.answer,...(translated.answerDisplay?{display:translated.answerDisplay}:{}),...(instance.answer.kind==="text"?{presentation:translated.answerDisplay}: {})},interaction,feedback:family.feedback};};
export const generateLocalizedUnit3FamilyInstance=(family,locale,options={})=>{assertSupportedLocale(locale);return localizeInstance(generateFamilyInstance(family,options),locale);};
