import { assertSupportedLocale } from "../../../i18n/config.js";
import { t } from "../../../i18n/index.js";
import { UNIT_6_EXERCISES } from "./exercises.js";
import exercisesEn from "./i18n/exercises.en.js";
import { getLocalizedUnit6ExerciseFamilies } from "./family-localize.js";
import { localizeExercisePresentation } from "../localize-exercise-presentation.js";
const required=(value,context)=>{if(value==null||value==="")throw new RangeError(`Missing English Unit 6 exercise translation: ${context}`);return value;};
const stageTexts=["Select the governing rotational relation and state the axis and sign convention.","Represent every quantity about the same origin or axis.","Substitute the data while preserving signs and units.","Check the result against the model conditions and a limiting case."];
export const localizeUnit6Exercise=(exercise,locale)=>{assertSupportedLocale(locale);if(!exercise||locale==="es")return exercise;const translated=required(exercisesEn[exercise.id],exercise.id);let interaction=exercise.interaction;if(interaction.kind==="singleChoice"){const options=required(translated.options,`${exercise.id}.options`);if(options.length!==interaction.options.length)throw new RangeError(`English Unit 6 options changed structure: ${exercise.id}`);interaction={...interaction,options:interaction.options.map((option,index)=>({...option,content:options[index]}))};}else if(interaction.kind==="number"){interaction={...interaction,field:{...interaction.field,label:t(locale,"exercise.answer")}};}const answer=exercise.answer.kind==="text"?{...exercise.answer,presentation:translated.options?.[interaction.options.findIndex(({id})=>id===interaction.correctOptionId)]}:{...exercise.answer};return{...exercise,title:translated.title,prompt:translated.prompt,objectives:[translated.title],prerequisites:[],hints:["State the axis, sign convention, and validity conditions before substituting."],solution:exercise.solution.map((step,index)=>({...step,title:["Principle","Representation","Calculation","Interpretation"][index]??`Step ${index+1}`,text:stageTexts[Math.min(index,3)]})),answer,interaction,feedback:{...exercise.feedback,correct:t(locale,"exercise.feedback.correct"),incorrect:t(locale,"exercise.feedback.incorrect")}};};
export const getLocalizedUnit6Exercises=(locale)=>UNIT_6_EXERCISES.map((exercise)=>localizeExercisePresentation(localizeUnit6Exercise(exercise,locale),locale));
export const getLocalizedUnit6BankItems=(locale)=>[...getLocalizedUnit6Exercises(locale),...getLocalizedUnit6ExerciseFamilies(locale)];
export { generateLocalizedUnit6FamilyInstance } from "./family-localize.js";
