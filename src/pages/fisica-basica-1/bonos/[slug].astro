---
import { getBonusesByUnit } from "../../../data/bonuses/index.js";
export function getStaticPaths() { return getBonusesByUnit(1).map(({ slug }) => ({ params: { slug }, props: { slug } })); }
const { slug } = Astro.props;
return Astro.redirect(`/fisica-basica-1/mini-quices/${slug}`, 301);
---
