---
import BaseLayout from "../../../layouts/BaseLayout.astro";
import OpenPractice from "../../../components/academic/OpenPractice.astro";
import UnitOneNav from "../../../components/academic/UnitOneNav.astro";
import { UNIT_1_BANK_ITEMS } from "../../../data/physics/unit-1/bank.js";
import { presentUnit1RichText } from "../../../data/physics/unit-1/math-content.js";
import { localizeUnit1 } from "../../../data/physics/unit-1/localize.js";
import { UNIT_1_VISUALIZATIONS } from "../../../data/physics/unit-1/visualizations.js";
import { ROUTE_IDS } from "../../../i18n/routes.js";
import { withBase } from "../../../utils/paths.js";

const { locale = "es" } = Astro.props;
const UNIT_1 = localizeUnit1(locale);

const publicLearningExercises = UNIT_1_BANK_ITEMS.filter(
  (exercise) =>
    exercise.status !== "draft" &&
    exercise.purpose === "learning" &&
    exercise.exposure === "public"
);
---

<BaseLayout
  title={`Práctica · ${UNIT_1.shortTitle}`}
  description={`Banco de ejercicios originales de ${UNIT_1.title}.`}
  locale={locale}
  routeId={ROUTE_IDS.COURSE_UNIT_1_PRACTICE}
>
  <article class="unit-practice-page">
    <header class="unit-topic-hero">
      <nav class="academic-breadcrumb" aria-label="Migas de pan">
        <a href={withBase("/fisica-basica-1/ejercicios")}>Ejercicios y tutorías</a>
        <span aria-hidden="true">/</span>
        <a href={withBase(UNIT_1.route)}>{UNIT_1.shortTitle}</a>
      </nav>

      <div class="unit-topic-hero__heading">
        <p class="eyebrow eyebrow--dark">Práctica · {UNIT_1.shortTitle}</p>
        <h1>Ejercicios de vectores y cinemática</h1>
        <p>
          Banco original para practicar de forma abierta. No es un motor
          evaluativo ni conserva respuestas o actividad personal.
        </p>
      </div>

      <aside class="practice-policy">
        <strong>Práctica sin seguimiento personal</strong>
        <p>
          Las pistas y soluciones se revelan con controles nativos. No se usa
          inteligencia artificial, backend, seguimiento ni almacenamiento local.
        </p>
      </aside>
    </header>

    <UnitOneNav locale={locale} />

    <OpenPractice
      exercises={publicLearningExercises}
      topics={UNIT_1.topics}
      visualizations={UNIT_1_VISUALIZATIONS}
      present={presentUnit1RichText}
    />

  </article>
</BaseLayout>
