{
  "name": "pagina-fisica",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "test": "node --test tests/*.test.mjs",
    "test:charts": "node --test tests/chart-utils.test.mjs",
    "validate": "node scripts/validate.mjs && npm test",
    "verify": "npm run validate && npm run build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^7.1.6"
  }
}
